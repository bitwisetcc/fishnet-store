/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "aquaticarts.com", "fishnet-api.onrender.com", "pixfeeds.com"],
  }
};

export default nextConfig;
